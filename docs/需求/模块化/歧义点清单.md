# 歧义点清单

> 所属项目：Gate · 模块化文档 v2.0  
> 状态：**已全部确认** ✓  
> 更新日期：2026-03-18

---

| # | 所属模块 | 歧义内容 | 方案 A | 方案 B |
|---|----------|----------|--------|--------|
| A1 | 全局代理管理 | `gate set` 是否同时写入 `ALL_PROXY` | 只写 `HTTP_PROXY`+`HTTPS_PROXY` | **✓ 同时写入 `ALL_PROXY`** |
| A2 | 全局代理管理 | Shell Hook 写入模式 | **✓ 动态读取（每次执行）** | 静态写入 |
| A3 | 全局代理管理 | `--expires` 是否支持混合单位如 `2d12h` | 只支持单一单位 `Nh`/`Nd` | **✓ 支持混合单位 `2d12h`、`1h30m`** |
| A4 | 工具代理管理 | `gate app --all` 操作范围 | 仅已安装工具 | **✓ 所有工具（含未安装，失败则跳过）** |
| A5 | 工具代理管理 | 写入时是否保留 BOM | **✓ 保留原 BOM** | 统一 UTF-8 无 BOM |
| A6 | 工具代理管理 | keyvalue 格式注释行是否保留 | **✓ 保留所有注释行** | 删除代理相关注释行 |
| A7 | 预设管理 | `preset load` 时工具已卸载的处理 | 静默跳过 | **✓ 警告并列出跳过的工具名** |
| A8 | 预设管理 | `preset save` 是否包含 `NO_PROXY` | **✓ 包含（快照完整 ProxyConfig）** | 不包含 |
| A9 | 连通性测试 | `--verify` 时 SSL 证书验证策略 | 忽略证书 | 验证证书 | 
| A10 | 连通性测试 | 无代理时 `gate test` 行为 | **✓ 报错退出，退出码 2** | 直接测试直连 |
| A11 | 插件系统 | 同名内置工具存在时是否需要 `--force` | 直接提示并覆盖 | **✓ 需要 `--force`，且提示用户** |
| A12 | CLI 命令 | `gate clear --all` 是否包含插件工具 | **✓ 包含（所有 ToolRegistry 工具，含插件）** | 仅内置工具 |
| A13 | 数据模型 | `proxyExpiresAt` 检测时机 | **✓ 仅在 `gate`/`gate info` 启动时检查** | 每次命令前检查 |
| A14 | MCP 服务器 | `verify: true` 失败的返回格式 | **✓ 返回 `result: { success: false, message }` ** | 返回 JSON-RPC error |
| A15 | MCP 服务器 | stdio 模式日志写入位置 | **✓ 日志写 `{DataDir}/mcp.log`** | 日志写 stderr |

---

## 确认明细

| # | 确认方案 | 确认日期 | 备注 |
|---|----------|----------|------|
| A1 | 方案 B | 2026-03-18 | `gate set` 同时写入 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` |
| A2 | 方案 A | 2026-03-18 | Shell Hook 动态读取，始终反映最新代理状态 |
| A3 | 方案 B | 2026-03-18 | `--expires` 支持混合单位：`2d12h`、`1h30m`、`30m` 等 |
| A4 | 方案 B | 2026-03-18 | `--all` 对所有工具尝试写入，未安装工具失败则跳过并在摘要报告 |
| A5 | 方案 A | 2026-03-18 | 读取原文件编码，写入时保留原 BOM |
| A6 | 方案 A | 2026-03-18 | keyvalue 格式原地更新时保留所有注释行 |
| A7 | 方案 B | 2026-03-18 | `preset load` 跳过已卸载工具时输出 WARN 并列出工具名 |
| A8 | 方案 A | 2026-03-18 | `preset save` 快照完整 ProxyConfig，包含 NO_PROXY |
| A9 | 两者结合 | 2026-03-18 | 默认忽略证书（兼容内网）；证书验证失败时给出 WARN 而非错误，不中止测试 |
| A10 | 方案 A | 2026-03-18 | 当前无代理时 `gate test` 报错退出，退出码 2 |
| A11 | 方案 B + 提示 | 2026-03-18 | 需要显式 `--force` 才能覆盖内置工具；安装时输出 WARN 告知用户内置工具将被覆盖 |
| A12 | 方案 A | 2026-03-18 | `gate clear --all` 包含插件工具（所有 ToolRegistry 中的工具） |
| A13 | 方案 A | 2026-03-18 | 过期检测仅在 `gate`/`gate info` 启动时执行，其他命令不检查 |
| A14 | 方案 A | 2026-03-18 | MCP `verify` 失败返回 `result: { success: false, message }` 而非 JSON-RPC error |
| A15 | 方案 A | 2026-03-18 | MCP stdio 模式日志写 `{DataDir}/mcp.log`；stderr 保持静默避免干扰客户端 |
